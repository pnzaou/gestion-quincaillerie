import { ArticleTableSkeleton } from "@/components/skeletons";

const Loading = () => {
    return (
        <div>
            <ArticleTableSkeleton/>
        </div>
    );
}

export default Loading;
